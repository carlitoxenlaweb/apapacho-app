import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.apapacho.com',
  appName: 'apapacho',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
